[variables]
NIXPACKS_PHP_ROOT_DIR = "/app/public"
NIXPACKS_PHP_EXTENSIONS = "ctype,curl,dom,fileinfo,filter,hash,intl,mbstring,openssl,pcre,pdo,session,tokenizer,xml,zip,pdo_mysql,redis"

[phases.setup]
aptPkgs = ["libicu-dev", "libzip-dev", "zip", "unzip", "git", "ca-certificates"]

[phases.install]
cmds = [
  "mkdir -p /usr/local/etc/php/conf.d",
  "install-php-extensions pdo_mysql mbstring bcmath intl zip opcache redis",
  "composer install --optimize-autoloader --no-interaction --no-scripts",
  "npm ci"
]

[phases.build]
cmds = [
  "npm run build",
  "php artisan event:cache",
  "php artisan config:cache",
  "php artisan route:cache",
  "php artisan view:cache"
]

[start]
cmd = "php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
